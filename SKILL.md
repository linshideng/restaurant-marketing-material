---
name: restaurant-marketing-material
description: 当用户需要生成/制作/产出/制造餐厅/餐饮营销物料时使用此技能。物料类型包括：营销海报、KT板、展架、易拉宝、台卡、传单、朋友圈海报、短视频封面、小程序Banner、美食头图、五连图。触发词包括但不限于：做海报、生成海报、制作海报、产出物料、做一张、帮我做、设计一个、来一张、出一版、搞一个、画一张、做个KT板、做展架、做易拉宝、做台卡、做传单、做五连图、餐饮海报、促销海报、活动海报、门店海报、营销物料、推广物料、宣传物料、物料设计、物料生成。通过后端生图 API（OpenAI GPT Image 2 / Seedance 2.0）生成高质量餐饮推广设计，同时保证二维码的可扫描性。

metadata:
  author: "linshideng"
  version: "V2"
---

# Restaurant Marketing Material

使用此技能生成餐饮营销物料草稿。物料类型支持营销海报、KT板、展架（易拉宝）、台卡、传单、朋友圈海报、短视频封面、小程序 Banner、美食头图、五连图。图像模型主导视觉设计；普通物料的二维码由本地后合成保证可扫描性，五连图没有二维码/QR 逻辑。

v4.1 工作流程：

1. **交互层**：确认物料类型、尺寸、二维码，以及是否需要真实菜品/产品图片。
2. **业务意图与设计风格层**：从用户请求识别 `scene.business_intent`（如新品、促销、节日、品牌形象、社交传播、日常引流等），再从 `templates/styles/` 的 25 个设计风格中推荐 Top 5 供用户选择（默认取前 2 个差异明显的方案生成）。代码验证 JSON、执行品类文化过滤和色彩/构图差异约束，必要时回退到本地规则，并写入 `template_selection.json`。
3. **海报 prompt 组装层**：从选中的模板组装紧凑的完整海报 prompt。该 prompt 要求后端生图 API 创建完整的商业海报，**仅渲染 `copy.selected_text` / `title` / `campaign.offer` / `campaign.cta` 中的用户确认文案**，在需要平台 Logo 时为本地 logo 叠加预留自然的品牌角落，并在需要二维码时设计一个视觉美观、内部干净近白色、正面朝向、轴对齐的二维码承载区域。prompt 中严禁将 `scene.intent`（如"日常引流"）、`copy.dimensions`、`style.name` 等内部标签写为画面可见文字。
4. **图像模型层**：使用后端生图 API（通过 `IMAGE_BACKEND` 环境变量选择，支持 OpenAI GPT Image 2 / Seedance 2.0）生成完整的 AI 海报为 `materials/material_XX_ai.png`。多变体生成使用 `ThreadPoolExecutor` 并行调用 API（最多 4 个并行）。
5. **二维码增强管线**（`qr_enhance.py` + `qr_composite.py`）— 普通物料存在二维码时执行；五连图跳过。二维码归一化和网格分析仅运行一次，结果在所有变体间共享：
   - **B1 归一化**：裁剪二维码源图的装饰边框，保留静默区 + 中心图标 → `qr_normalized.png`。
   - **B2 网格分析**：通过频域分析检测二维码版本和模块数量。
   - **B3 尺寸计算**：计算目标二维码尺寸（短边的 23–28%），然后对齐到整数模块倍数以实现像素级精确渲染。
   - **B4 插槽检测**：通过连通分量分析亮区域找到 AI 生成的二维码承载区域；过滤误检；边缘收缩到干净内边界；根据亮度和拉普拉斯方差评分。
   - **B5 构建放置方案**：三种适配模式 — `detected_slot`（直接粘贴到干净插槽）、`soft_slot`（半透明底衬）、`fallback_card`（传统白色圆角卡片托盘 + 阴影）。如果未检测到插槽则回退到程序评分放置。
   - **C 合成**：`qr_composite.py` 使用 **NEAREST 插值**粘贴二维码（不对黑白模块做抗锯齿模糊），带亮度保护（如果区域亮度 < 200 则添加白色底衬）。强制 PNG 输出。
   - **D 验证**：可选的扫描验证，通过 zbarimg / pyzbar / cv2.QRCodeDetector 对比源二维码和最终合成图的解码内容。
6. **审查层**：记录二维码哈希、适配模式、网格对齐参数、插槽指标、扫描验证结果和人工验证项。`review.md` 包含每个变体可直接运行的 `qr_composite.py` 命令及 `--fit-mode` 参数。

二维码绝不能由图像模型生成或重绘。二维码图像不会被重新着色或重绘；它仅被等比缩放并原样粘贴。

图像模型负责构图整张海报。它可以渲染少量用户提供的精确展示文案以增强冲击力，但不得捏造价格、日期、规则、门店信息、产品信息或二维码事实。

**⚠️ 禁止渲染内部元数据到画面上**：以下字段是系统内部分类标签，**绝不能**作为文案文字出现在海报画面中：
- `scene.intent` / `scene.business_intent` 的值（如"日常引流"、"推新品"、"促销打折"、"品牌形象"、"社交传播"等）
- `copy.dimensions` 的值（如"餐品特色"、"消费场景召唤"等）
- `style.name` / `style.cuisine_tag` / `style.city_tag` 的值
- 任何 JSON 字段名或内部 ID（如 `daily_attract`、`bold_split`）

海报画面上只能出现 `copy.selected_text`（用户确认的展示文案）、`campaign.offer`、`campaign.cta`、`title` 中的内容。如果用户没有提供展示文案，宁可画面不放文字（纯氛围图），也不能把内部标签当文案渲染上去。

## 内容安全审核

在进入交互关卡之前，必须先对用户的物料请求进行内容安全审核。如果用户提供的标题、文案、主题或任何描述内容涉及以下类别，**必须明确告知用户调整内容，并婉拒生图请求**：

- **暴力**：含血腥、武器、伤害、威胁性语言
- **歧视**：种族、性别、地域、宗教、残障等歧视性内容
- **色情**：露骨的性暗示、低俗擦边内容
- **毒品**：涉及违禁药品、吸毒暗示
- **政治**：涉及政治敏感人物、事件、口号、党派宣传
- **违法**：赌博、传销、非法集资等违法活动宣传
- **虚假宣传**：明显违反广告法的绝对化用语（如"最好""第一""国家级"）

另有 **受保护 IP 风险**，不直接婉拒，优先自动规避：物料严禁出现未经授权的赛事、联盟、影视动画、游戏动漫等专有名称、标志性文案或图形。例如世界杯/FIFA/大力神杯、NBA/球队队徽球衣、奥运五环/吉祥物、迪士尼/米奇/城堡、漫威/复仇者联盟、星球大战、哈利波特、宝可梦/皮卡丘、Hello Kitty、哆啦A梦等。用户输入命中时，必须替换为"看球""球赛""篮球赛""运动会""童趣""超级英雄""魔法主题"等通用中性表达，并简要告知已做合规替换；如果用户坚持保留专有 IP，再婉拒生成。

拒绝话术示例："您提供的文案内容涉及[具体类别]，不适合用于营销物料。建议调整为更积极正面的表达，我可以帮您重新拟定文案。"

审核通过后方可继续后续流程。

## 交互关卡

生成前采用两屏渐进式引导，降低新用户填写焦虑。核心原则：**2 步内可触发生成**，增强项不阻断流程。

**⚠️ 全流程禁止输出内部推理（最高优先级）**：在整个 Skill 执行期间（从首轮交互到生成完毕），Agent 的回复中**绝不能**出现以下内容：
- 英文或中文的自言自语式推理（如 "Actually, the user said no QR..."、"Let me try a different approach..."、"No wait, the skill says..."、"Rethinking — ..."）
- 内部决策过程（如 "台卡 layouts require QR, I should inform..."、"The best approach: since the user said..."）
- 对 Skill 规则的引用或解读（如 "the manifest says..."、"the skill says台卡 can skip QR"）
- 自我纠正对话（如 "Actually..."、"No wait..."、"Let me reconsider..."）
- 任何不是直接面向用户的信息

Agent 应该**只输出面向用户的最终结论**。如果需要做决策（如二维码是否跳过、比例如何调整），直接执行，不要把决策过程写给用户看。如有需要告知用户的注意事项，用一句简短中文说明即可。

**交互方式适配**：根据平台能力选择交互方式：
- 有 AskQuestion 工具（CatDesk/IDE）→ 使用结构化选项卡，第一屏和第二屏分别用 AskQuestion 收集（见下方"AskQuestion 交互示例"）
- 纯聊天环境 → 走纯文本方式（见下方"聊天场景完整流程"）

### 聊天场景完整流程

聊天环境及其他无 AskQuestion 环境下，所有交互通过纯文本完成。**严格 2 轮对话**完成所有收集。

**话术原则**：
- **简洁**：直接给选项，不输出思考过程、不解释为什么要收集、不加冗余客套
- **分层结构化**：每个大项独占一行，子选项缩进或字母标注，层次分明，不要挤成一段
- **可快速回复**：提供"回复示例"让用户一句话搞定

### AskQuestion 交互示例

**第 1 屏 — 基础选项**（必须发出，不可跳过）：

```yaml
header: "物料基础"
question: "选一下物料的尺寸和展示选项："
options:
  - {label: 营销海报 3:4, description: "门店装修、活动展示"}
  - {label: 展架/易拉宝 2:5, description: "门口展架/立牌"}
  - {label: KT板 4:3, description: "灯箱/挂牌"}
  - {label: 朋友圈海报 9:16, description: "社交传播"}
  - {label: 台卡 4:3, description: "桌面展示"}
  - {label: 传单 2:3, description: "派发传单"}
  - {label: 小程序 Banner 16:9, description: "线上推广"}
  - {label: 短视频封面 9:16, description: "视频推广"}
  - {label: 美食头图 1:1, description: "图文展示"}
  - {label: 五连图 20:3, description: "门店装修五张"}
```

**第 2 屏 — 风格与文案**（有风格参考图时只展示文案候选和口味提示，不展示风格选项）：

```yaml
header: "推荐文案"
question: "选择文案方向或直接输入你的文案："
options:
  - {label: 默认文案 A, description: "地道美味 新鲜现做"}
  - {label: 默认文案 B, description: "烟火气 好味道"}
  - {label: 默认文案 C, description: "一口惊艳 回味无穷"}
  - {label: 自定义, description: "输入你的专属文案"}
  - {label: 无需文字, description: "纯氛围图"}
```

**⚠️ 排版强制要求**（纯文本场景）：无风格参考图时，每个风格选项**必须独占一行**，每条文案候选**必须独占一行**。有风格参考图时，不输出风格选项，只保留参考图确认语和文案候选。风格列表和文案列表之间用**空行分隔**。严禁把多个选项挤在同一行。

**第 2 轮只输出**：无风格参考图时输出风格选项列表 + 文案选项列表 + 画面口味提示（如有必要）+ 回复指引，用户看到的第一行就是"我推荐这 5 个风格..."。有风格参考图时只输出参考图确认语 + 文案选项列表 + 画面口味提示（如有必要）+ 回复指引，第一行必须是"已收到参考图，我会按参考图整体设计对标生成，不再让你选风格。"，严禁出现"我推荐这 5 个风格"、A-E 风格选项或任何预设风格名称。直接以中文选项开头，不要有任何铺垫、解释、思考痕迹。

**第 2 轮用户回复解析（必须立即执行，不再追问）**：
- 只回"OK"、"可以"、"开始"、"生成"：有风格参考图时按 `style_reference_dominant` + 默认文案生成；无风格参考图时，普通物料按默认前 2 个风格 + 默认文案生成，五连图按第 1 个默认风格 + 默认文案生成。
- 回复含 `1-5`、"第 N 条"、"文案 N"、"文案用 N"：按用户指定文案生成；有风格参考图时仍固定使用 `style_reference_dominant`；无风格参考图且未指定风格时，普通物料用默认前 2 个风格，五连图用默认第 1 个风格。
- 无风格参考图时，回复含 `A-E`、"换成 A+C"、"风格 A+C" 等：按用户指定风格生成；未指定文案时，使用默认文案。
- 有风格参考图时，回复含 `A-E`、"换成 A+C"、"风格 A+C" 等：忽略风格字母，继续使用 `style_reference_dominant`；如果用户文字里有明确文案编号或自定义文案，只更新文案。
- 回复是一段自然文案，且不包含明确风格字母或文案编号：视为自定义展示文案；有风格参考图时按 `style_reference_dominant` 生成；无风格参考图时，普通物料按默认前 2 个风格生成，五连图按默认第 1 个风格生成，并把用户整句写入 `copy.selected_text`。

**用户在第 1 轮说"默认"时**：第 1 轮所有选项按默认值填充。有风格参考图时不展示 Top 5 推荐风格，只做参考图确认和文案候选；无风格参考图时不跳过第 2 轮，仍然正常展示 Top 5 推荐风格 + 文案候选，让用户选择或确认后再生成。

**智能跳过原则**：用户首消息已经明确的答案不要重复追问；缺失项先按默认值补齐。

**交互轮次硬限制（所有平台通用）**：从用户发起请求到开始生成，最多 2 轮交互。严格禁止第 3 轮。

**上下文自动预填规则**：
- 用户消息中已包含菜系信息（如"贵州豆豉火锅"）→ 自动预填 `cuisine_tag`，第二屏不再询问菜系
- 用户消息中已包含地域信息（如"贵州"）→ 自动预填 `city_tag`，第二屏不再询问地域
- 无转化语义（无扫码/团购/优惠/价格关键词）→ 直接设置 `qr_code_not_needed=true`，不询问二维码
- 用户消息中已包含具体尺寸或物料类型暗示尺寸 → 自动预填，第一屏可跳过

**设计风格 style_id 安全规则**：
- 禁止自行翻译或编造 `style_id`。无风格参考图时必须先调用 `--recommend-styles 5` 获取合法 id 列表，用返回的 id 构建选项。
- 有风格参考图时，唯一合法视觉模式是 `style_reference_dominant`；不要展示给用户选择，不要把它作为一个可选项让用户点选，不要传 `--pre-selected-styles`。
- 无风格参考图时，用户确认的 id 直接传入 `--pre-selected-styles`，不做任何转换。
- 无风格参考图时，如果用户要求"换一个"或输入自定义描述，Agent 从推荐列表的后续合法 style 中替换；仍无法匹配时使用 `--template-selector-provider none` 走本地规则，不把自定义文字当作 `style_id`。

**目录与文件操作规则**：写 request.json 前必须确保目录存在（`mkdir -p dir && cat > dir/request.json`），不允许先写失败再补建目录。

### 进入前：用户频次与品牌记忆

如果 Agent memory 可用，进入交互前检查本 Skill 的用户使用次数：

- 使用次数 `< 3` 或没有记录：走第一屏 → 第二屏的渐进模式，第二屏可跳过。
- 使用次数 `>= 3`（老用户识别）：默认展开完整填写模式（物料类型、尺寸、门店特色、文案、推荐设计风格、二维码一屏收集）。
- 如果用户提到品牌名且 memory 中有同品牌 `brand_profile`，自动填入请求；不要在仓库内创建本地用户数据库。

每次成功生成物料后，更新 memory 中本 Skill 的使用计数；若本次用户明确保存品牌主色、关键词或 logo 锁定位，也写入该品牌的 `brand_profile`。

### 第一屏（必填）：最小可生成请求

用户只需提供 **物料类型** 即可触发生成。Agent 补齐其余默认值：

1. **物料类型**：`营销海报`、`KT板`、`展架`（含易拉宝）、`台卡`、`传单`、`朋友圈海报`、`短视频封面`、`小程序 Banner`、`美食头图`、`五连图`。
2. **尺寸**：按物料类型自动填入默认比例（营销海报 `3:4`、KT板 `4:3`、展架/易拉宝 `2:5`、台卡 `4:3`、传单 `2:3`、朋友圈海报 `9:16`、短视频封面 `9:16`、小程序 Banner `16:9`、美食头图 `1:1`、五连图 `20:3`）。用户可选择预设比例，或输入自定义宽×高像素（优先，如 `1242×1660px`、`宽1242高1660像素`）；印刷物料也兼容自定义毫米。五连图固定一次性生成 `20:3` 长图后切成 5 张 `2048×1536px` 交付，完整长图尺寸为 `10240×1536px`。
3. **门店名称**（三选一）：
   - 自定义名称：用户输入真实门店/品牌名，用于理解品牌语境和辅助文案；是否展示取决于用户选择的主文案/标题，不承诺默认渲染到海报上
   - 通用名称：不展示具体店名，海报仅呈现文案和氛围
   - 跳过：不放店名，后续可补
   
   如果用户消息中已提及门店名，自动预填为"自定义名称"，不再询问。
4. **标题**：用户不需要手写 `title`。Agent 从候选文案、`campaign.theme` 或门店名推断短标题；提交脚本时 `title` 仍必须有值。
5. **QR**：未提及 QR 且未命中转化语义时，默认 `assets.qr_code_not_needed=true`。命中"扫码/二维码/小程序/加群/核销/下单"等关键词，或包含价格、折扣、团购、优惠等转化语义时，主动引导上传 QR。
6. 无 QR 时，文案候选和最终 `campaign.cta` 不得包含"扫码/扫一扫/二维码/小程序码"等动作。

五连图特殊规则：
- 支持上传 0-7 张实拍图，`assets.reference_images` 可包含菜品、门店环境、服务场景或空间细节；超过 7 张直接报错。
- **五连图禁止虚构菜品**：若用户未上传任何菜品实拍图，画面中严禁出现任何具体的盘装菜品、碗装食物、锅装菜肴或可识别的食物主体。无实拍图时应使用氛围元素（蒸汽、餐具、灯光、食材剪影、品类象征图形等）代替菜品，营造餐饮氛围但不具体呈现菜品。
- 多张实拍时，默认第 1 张是最重要实拍图，prompt 会要求模型优先围绕它组织核心卖点。
- **菜品数量显式枚举约束**：当用户提供 `products[].reference_image_index` 时，只把被有效索引引用的参考图视为菜品实拍图；prompt 中必须逐一命名并要求模型画出全部 N 道菜（写成等式"exactly N dishes"）。有菜名时使用用户提供的原文菜名，无菜名但被标记为菜品图时使用 `dish from reference photo #N`；非菜品实拍（门店环境、服务场景、Logo、空间细节等）不计入 N，只作为 ambient/environment reference 使用。用户选择跳过菜名/不标菜名时，保留菜品图的 `reference_image_index` 但不要写入 `products[].name`，确保只展示菜品、不生成菜名标签。未提供有效映射的旧请求沿用现有参考图展示逻辑。

### 第二屏（增强选项）

**设计风格选择**（无 `assets.style_reference_images` 时）：
- 调用 `python3 scripts/material_skill.py --recommend-styles 5 --request-file materials/request.json`，取返回的 Top 5 风格 id
- 展示给用户时，用风格对应的 `label`、`material_snapshot` 和 `suggest` 构建选项文案

**文案推荐**：
- 调用 `python3 scripts/material_skill.py --recommend-copy --request-file materials/request.json`，取返回的文案候选项
- 每条文案候选用 `copy.selected_text` 或 `copy.suggest` 展示

**QR 上传**：普通物料支持上传用户的二维码图片，默认 4 选 1：
- 用户自备 QR：直接上传图片
- 展示默认店铺二维码（有品牌素材或历史记录时）
- 提供扫码目标链接：Agent 自动生成二维码
- 不需要二维码：跳过 QR

**菜品/产品实拍图**（可选，仅当用户有实拍时上传）：
- 上传实拍菜品图片可让 AI 识别菜品形态和摆盘，生成的海报更真实
- 五连图建议上传 1-7 张实拍图以获得最佳效果

该技能同时处理**普通物料**和**五连图**两类基本场景：
- **普通物料**：支持单张海报生成（可能含 QR）
- **五连图**：固定 5 张 2048x1536 的切片序列

> 五连图差异：固定 `20:3` 全景长图后切片 `2048x1536`，不支持自定义像素参数。

二维码不可用且 `qr_code_not_needed` 为 false 时收到二维码的用户处理流程：
- 立即告知用户此技能需要用户提供二维码，并把图片发给用户；不要自行生成假二维码，不要告诉用户"生成后自行添加"等无用建议

### request.json 完整字段

```json
{
  "title": "3-5 字短标题（必须填写，用作生图脚本文件名前缀和海报标题占位）",
  "material_type": "营销海报|KT板|展架|台卡|传单|朋友圈海报|短视频封面|小程序Banner|美食头图|五连图",
  "size": { "width": 1242, "height": 1660 },
  "dpi": 300,
  "scene": {
    "cuisine_tag": "火锅|川菜|粤菜|日料|西餐|烘焙|咖啡|茶饮|烧烤|韩餐|东南亚|西北|江浙菜|东北菜|湘菜|鲁菜|闽菜|徽菜|鄂菜|其他",
    "city_tag": "",
    "business_intent": "推新品|促销打折|节日营销|品牌形象|社交传播|日常引流|会员活动|开业推广|周年庆|外卖推广|季节性上新|活动清仓"
  },
  "campaign": {
    "theme": "可选：活动主题描述",
    "offer": "可选：具体优惠信息（如"满100减20"、"第二份半价"）",
    "cta": "可选：行动号召（如"立即下单"、"马上抢购"）"
  },
  "copy": {
    "selected_text": "海报上展示的主文案（由用户从候选文案中选择或自定义）",
    "dimensions": ["可选：餐品特色", "可选：消费场景召唤", "可选：情感共鸣点", "可选：价值承诺"],
    "title_display_mode": "text_primary|text_only_primary|hide"
  },
  "assets": {
    "reference_images": [],
    "style_reference_images": [],
    "mascot_image": null,
    "qr_code": null,
    "qr_code_not_needed": false,
    "platform_logo": "auto|none"
  },
  "products": [
    {
      "name": "菜品名称",
      "price": "菜品价格",
      "reference_image_index": 0,
      "description": "可选：菜品简短描述"
    }
  ],
  "user_original_prompt": ["触发本次生图的用户原始输入"],
  "delivery_run_id": "自动生成的运行 ID"
}
```

### 字段说明

- `material_type`（必填）：物料类型。影响默认画布比例、模板选择、prompt 约束、二维码处理、生成尺寸和输出格式。直接决定画布方向、参考图索引和风格参考图放置。
- `scene.cuisine_tag`：菜系标签，用于视觉参考和风格约束。如果没有显式指定，可以从 `user_original_prompt` 推断。
- `scene.city_tag`：城市标签，可选。用于某些文化敏感的风格调整（如重庆火锅 vs 潮汕牛肉火锅）。
- `copy.selected_text`：最终选定要在海报中渲染的展示文案。这是图像模型需要在画面中呈现的精确文本字符串。绝对不能包含系统内部标签。必须是用户确认过的文本。
- `copy.title_display_mode`：标题展示模式。`text_primary`：主文案以标题形式展示；`text_only_primary`：画面只放主文案，不放额外文字；`hide`：画面不放任何文字。
- `assets.platform_logo`：平台 Logo 选择。`auto` 自动选择；`none` 不展示 logo。
- `user_original_prompt`（强烈建议填写）：**必须包含触发本次生图的原始输入**（如"生成一个韩式炸鸡店的营销海报"），并**建议追加各轮澄清时用户的回答**，让记录更全面。

**⚠️ 构建 request.json 时必须写入 `user_original_prompt`**：把触发本次生图的用户原始输入（如"生成一个韩式炸鸡店的世界杯营销海报"）作为数组第 1 条，各轮澄清时用户的回答依次追加为后续条目。

## 真实菜品参考图处理流程

> **核心原则**：模型使用真实菜品图片作为视觉参考生成海报，而非替换为 AI 虚构菜品。模型看到的应该是"用户提供的真实菜品"，而非 AI 生成的"看起来像"的食物。

### 参考图来源

1. 用户上传：`assets.reference_images` 中的实拍图片，需 Base64 编码传递给脚本
2. 已有参考图且用户选择使用：复用已有参考图
3. 用户未上传且无已有参考图：走标准无参考图生成流程

### 流程说明

1. **分发参考图**：请求中的 `assets.reference_images`（Base64 编码并标记 role）和 `products[].reference_image_index` 决定了哪些图作为菜品参考图发送给模型，数量上限 7 张。超出部分报错，不应从非授权来源下载图像。
2. **菜品展示**：当有菜品参考图时，prompt 中要求模型以**真实菜品**为主视觉焦点，保持菜品外观、颜色和摆盘与参考图保持一致。
3. **无参考图时的限制**：若用户未上传任何菜品实拍图，画面中严禁出现任何具体的盘装菜品、碗装食物、锅装菜肴或可识别的食物主体。此时应使用氛围元素代替菜品。

## 品牌 Logo 叠加

海报图像生成后，脚本会按需求在指定位置叠加平台 Logo：

- `auto` 下：自动选择最合适的 logo 素材
- `none`：不叠加任何 logo
- 可选展示其他品牌 logo（需品牌方提供素材）

字体说明：CSS 和本地叠加使用 `assets/fonts/` 下的字体文件。

## 交付物

`status.json` 最终状态：

```json
{
  "status": "done",
  "run_id": "auto-generated-run-id",
  "files": ["materials/variant_01.png", "materials/variant_02.png"],
  "variants": [
    {
      "id": "01",
      "style_id": "variant_id",
      "qr_placement": { "x": ..., "y": ..., "size": ... }
    }
  ]
}
```

`review.md` — 输出包含直接可运行的 QR 复合命令，Agent 可在人工审核后运行。

**⚠️ 交付后的人工审核**：图片交付后，**必须**附带人工审核提醒，要求用户对最终物料做全面检查，尤其重点检查文字和菜品。

## 后端/API 配置

本技能默认使用 **OpenAI GPT Image 2** 作为生图后端，也支持 **Seedance 2.0**。

```bash
# OpenAI（默认）
export OPENAI_API_KEY="sk-..."
export IMAGE_BACKEND="openai"

# Seedance 2.0
export SEEDANCE_API_KEY="..."
export SEEDANCE_BASE_URL="https://api.seedance.ai/v1"
export IMAGE_BACKEND="seedance"
```

**关键约束**：
- 图像生成使用配置的后端 API 能力，不兼容其他生图通道
- 模板选择器（文本提示推荐）复用同一后端的文本能力
- 当 API 不可用时回退到本地确定性规则

脚本通过以下 CLI 参数控制：

```bash
# 后端选择
--image-provider openai         # OpenAI GPT Image 2（默认）
--image-provider seedance       # Seedance 2.0
--image-provider none            # 仅生成 prompt，不调用生图 API

# 模板选择器
--template-selector-provider auto    # 优先 API 选择
--template-selector-provider api      # 强制 API 选择
--template-selector-provider none     # 本地规则选择

# 测试时使用 mock 图片
--mock-images
```

### 判断生图是否走 API

- **是**：调用了配置的 API（OpenAI GPT Image 2 / Seedance 2.0）
- **否**：回退到了其他生图方式或仅生成了 prompt

**简易判断规则**：只要最终出图不是通过配置的后端 API 完成的，一律填"否"并注明实际方式。

### 人工审核

图片生成完成后，Agent 必须：
1. **交付图片**：使用平台文件工具发送最终图片给用户
2. **发送审核提醒**：图片发送完成后，继续在对话里发送交付审核提醒文本。提醒内容至少包含"请对最终物料做全面检查，尤其重点检查文字和菜品"。
